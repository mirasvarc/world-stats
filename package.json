{
  "name": "world-stats-map",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt build && wrangler dev",
    "postinstall": "nuxt prepare",
    "deploy": "nuxt build && wrangler deploy",
    "cf-typegen": "wrangler types",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "leaflet": "^1.9.4",
    "nuxt": "^3.13.0",
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@nuxtjs/i18n": "^10.4.0",
    "@types/leaflet": "^1.9.21",
    "vitest": "^4.1.9",
    "wrangler": "^3.99.0"
  }
}
